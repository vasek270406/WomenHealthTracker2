# 🔨 Как пересобрать приложение

## Способ 1: Через кнопку Run (Самый простой)
1. Нажмите зеленую кнопку **▶️ Run** на панели инструментов
2. Приложение автоматически пересоберется и установится на устройство

## Способ 2: Через Gradle панель
1. Откройте панель **Gradle** справа (если не видно: View → Tool Windows → Gradle)
2. Разверните: **YourProject** → **Tasks** → **build**
3. Дважды кликните на **`build`** или **`assembleDebug`**

## Способ 3: Через терминал в Android Studio
1. Откройте встроенный терминал: **View → Tool Windows → Terminal**
2. Выполните команды:
   ```bash
   ./gradlew clean
   ./gradlew assembleDebug
   ```
3. APK файл будет в: `app/build/outputs/apk/debug/app-debug.apk`

## Способ 4: Горячие клавиши
- **Mac**: `Cmd + F9` (сборка) или `Ctrl + R` (запуск)
- **Windows/Linux**: `Ctrl + F9` (сборка) или `Shift + F10` (запуск)

## Способ 5: Через меню (если есть)
- **Build** → **Make Project** (`Cmd + F9` / `Ctrl + F9`)
- Или **Run** → **Run 'app'** (`Shift + F10` / `Ctrl + R`)

---

**Самый простой способ:** Просто нажмите кнопку **▶️ Run** — приложение пересоберется автоматически!






