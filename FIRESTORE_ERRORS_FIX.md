# 🔧 Решение ошибок подключения к Firestore

## ❌ Возможные причины ошибок:

### 1. **Правила безопасности Firestore слишком строгие**

Самая частая причина - правила безопасности блокируют доступ.

### 2. **Пользователь не авторизован**

Firestore требует авторизации для доступа к данным.

### 3. **SHA-1 не добавлен в Firebase Console**

Без SHA-1 Firebase не может проверить подлинность приложения.

### 4. **Интернет-соединение**

Эмулятор или устройство не имеет доступа к интернету.

---

## ✅ Решение 1: Проверить правила безопасности Firestore

### Шаг 1: Откройте Firebase Console
1. https://console.firebase.google.com/
2. Выберите проект **"womenht2"**
3. Перейдите в **Firestore Database**

### Шаг 2: Проверьте правила безопасности
1. Нажмите на вкладку **"Rules"**
2. Убедитесь, что правила выглядят так:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для пользователей
    match /users/{userId} {
      // Пользователь может читать и писать только свои данные
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Подколлекция: данные дня (days)
      match /days/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: данные беременности (pregnancy)
      match /pregnancy/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: данные фертильности (fertility)
      match /fertility/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: симптомы менопаузы (menopause_symptoms)
      match /menopause_symptoms/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: визиты к врачу при менопаузе (menopause_doctor_visits)
      match /menopause_doctor_visits/{visitId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: результаты MRS (mrs_results)
      match /mrs_results/{testDate} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
```

### Шаг 3: Опубликуйте правила
1. Нажмите **"Publish"** (Опубликовать)
2. Подождите несколько секунд

---

## ✅ Решение 2: Для тестирования (временно)

Если нужно быстро протестировать, можно временно разрешить доступ (⚠️ **ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ!**):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

**⚠️ ВАЖНО:** После тестирования верните безопасные правила!

---

## ✅ Решение 3: Проверить авторизацию

Убедитесь, что пользователь авторизован перед использованием Firestore:

```kotlin
val authHelper = FirebaseAuthHelper(this)
if (!authHelper.isUserLoggedIn()) {
    // Пользователь не авторизован
    // Перенаправить на экран входа
    return
}
```

---

## ✅ Решение 4: Добавить обработку ошибок

Проверьте, что все вызовы Firestore имеют обработку ошибок:

```kotlin
firestoreHelper.saveDayData(
    userId = userId,
    dayData = dayData,
    onSuccess = {
        // Успех
    },
    onError = { error ->
        Log.e("TAG", "Ошибка Firestore: $error")
        // Показать пользователю сообщение
    }
)
```

---

## ✅ Решение 5: Проверить интернет-соединение

1. Убедитесь, что эмулятор/устройство подключено к интернету
2. Проверьте настройки сети эмулятора
3. Попробуйте открыть браузер в эмуляторе и зайти на сайт

---

## 🔍 Как проверить, что проблема решена:

1. **Проверьте логи в Android Studio:**
   - Откройте **Logcat**
   - Найдите сообщения с тегом **"FirestoreHelper"**
   - Не должно быть ошибок **"PERMISSION_DENIED"**

2. **Проверьте Firebase Console:**
   - Firestore Database → Data
   - Должны появиться данные после сохранения

3. **Попробуйте зарегистрировать пользователя:**
   - Если регистрация проходит успешно, Firebase работает
   - Проверьте, что пользователь появился в Authentication → Users

---

## 📋 Чек-лист для проверки:

- [ ] SHA-1 добавлен в Firebase Console
- [ ] Правила безопасности Firestore опубликованы
- [ ] Authentication → Email/Password включен
- [ ] Firestore Database создана
- [ ] Пользователь авторизован перед использованием Firestore
- [ ] Интернет-соединение работает
- [ ] google-services.json на месте

---

## 🚨 Частые ошибки:

### "PERMISSION_DENIED"
**Причина:** Правила безопасности блокируют доступ
**Решение:** Проверьте правила Firestore (Решение 1)

### "UNAUTHENTICATED"
**Причина:** Пользователь не авторизован
**Решение:** Убедитесь, что пользователь вошел в систему

### "DEVELOPER_ERROR"
**Причина:** SHA-1 не добавлен
**Решение:** Добавьте SHA-1 в Firebase Console

### "Network error"
**Причина:** Нет интернет-соединения
**Решение:** Проверьте подключение к интернету

---

**После исправления правил перезапустите приложение!**


