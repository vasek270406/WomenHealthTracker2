# 📘 Пошаговая инструкция по настройке Firebase Console

## 🎯 Шаг 1: Вход в Firebase Console

1. Откройте браузер и перейдите по адресу: **https://console.firebase.google.com/**
2. Войдите в свой Google аккаунт (тот же, который использовали при создании проекта)
3. Выберите ваш проект **"womenht2"** (или создайте новый, если нужно)

---

## 🔐 Шаг 2: Включение Firebase Authentication

### 2.1. Переход в раздел Authentication

1. В левом меню найдите и нажмите на **"Authentication"** (или "Аутентификация")
2. Если видите сообщение "Get started" - нажмите на него

### 2.2. Включение метода Email/Password

1. В верхней части страницы нажмите на вкладку **"Sign-in method"** (или "Способы входа")
2. Вы увидите список доступных методов входа
3. Найдите в списке **"Email/Password"** и нажмите на него
4. В открывшемся окне:
   - Переключите переключатель **"Enable"** (Включить) в положение **ON** (включено)
   - Оставьте **"Email link (passwordless sign-in)"** выключенным (если не нужен)
5. Нажмите кнопку **"Save"** (Сохранить)

✅ **Готово!** Authentication теперь включен для вашего приложения.

---

## 💾 Шаг 3: Создание Firestore Database

### 3.1. Переход в раздел Firestore

1. В левом меню найдите и нажмите на **"Firestore Database"** (или "База данных Firestore")
2. Если видите кнопку **"Create database"** (Создать базу данных) - нажмите на неё

### 3.2. Выбор режима безопасности

1. Вам предложат выбрать режим:
   - **"Start in production mode"** (Начать в режиме production) - более строгие правила
   - **"Start in test mode"** (Начать в тестовом режиме) - для разработки
   
2. **Для начала выберите "Start in test mode"** (можно изменить позже)

### 3.3. Выбор расположения базы данных

1. Выберите регион для хранения данных (например, **"us-central"** или ближайший к вам)
2. Нажмите **"Enable"** (Включить)
3. Дождитесь создания базы данных (обычно 1-2 минуты)

✅ **Готово!** Firestore Database создана.

---

## 🔒 Шаг 4: Настройка правил безопасности Firestore

### 4.1. Переход к правилам

1. В разделе **"Firestore Database"** перейдите на вкладку **"Rules"** (Правила)
2. Вы увидите текущие правила безопасности

### 4.2. Установка правил безопасности

1. **ВАЖНО:** Удалите всё содержимое поля правил (если там что-то есть)
2. Скопируйте и вставьте следующий код (начинайте сразу с `rules_version`, без слова "javascript"!):

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для коллекции users
    match /users/{userId} {
      // Разрешаем чтение и запись только авторизованному пользователю для своих данных
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Подколлекция: данные дня (days)
      match /days/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: данные беременности (pregnancy)
      match /pregnancy/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: данные фертильности (fertility)
      match /fertility/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: симптомы менопаузы (menopause_symptoms)
      match /menopause_symptoms/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: визиты к врачу при менопаузе (menopause_doctor_visits)
      match /menopause_doctor_visits/{visitId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Подколлекция: результаты MRS (mrs_results)
      match /mrs_results/{testDate} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
```

3. **Проверьте:** Первая строка должна быть `rules_version = '2';`, а НЕ `javascript`
4. Нажмите кнопку **"Publish"** (Опубликовать)
5. Подтвердите публикацию правил

✅ **Готово!** Правила безопасности настроены.

---

## 📊 Шаг 5: Проверка структуры данных (опционально)

После того, как пользователь зарегистрируется и начнет использовать приложение, вы сможете увидеть данные в Firestore:

1. Перейдите в **"Firestore Database"** → вкладка **"Data"** (Данные)
2. Вы увидите структуру:
   ```
   users/
     {userId}/
       name: "Имя пользователя"
       age: 25
       cycleLength: 28
       ...
       days/
         {date}/
           date: "2024-11-18"
           mood: 3
           ...
   ```

---

## ✅ Проверка готовности

Убедитесь, что выполнены все шаги:

- [ ] ✅ Authentication включен
- [ ] ✅ Email/Password метод активирован
- [ ] ✅ Firestore Database создана
- [ ] ✅ Правила безопасности установлены и опубликованы

---

## 🚨 Частые проблемы и решения

### Проблема 1: "Authentication not enabled"
**Решение:** Убедитесь, что вы включили метод Email/Password в разделе Authentication → Sign-in method

### Проблема 2: "Permission denied" при сохранении данных
**Решение:** 
- Проверьте, что правила безопасности опубликованы
- Убедитесь, что пользователь авторизован в приложении
- Проверьте, что userId в правилах совпадает с текущим пользователем

### Проблема 3: База данных не создается
**Решение:**
- Убедитесь, что у вас есть права на создание ресурсов в проекте
- Попробуйте выбрать другой регион
- Проверьте, нет ли ограничений на аккаунте

### Проблема 4: Ошибка "Missing or insufficient permissions"
**Решение:**
- Проверьте правила безопасности Firestore
- Убедитесь, что пользователь авторизован (`request.auth != null`)
- Проверьте, что userId совпадает (`request.auth.uid == userId`)

---

## 📱 Тестирование

После настройки:

1. Запустите приложение
2. Зарегистрируйте нового пользователя
3. Проверьте в Firebase Console:
   - В **Authentication** → **Users** должен появиться новый пользователь
   - В **Firestore Database** → **Data** должны появиться данные пользователя

---

## 🔄 Следующие шаги

После успешной настройки:

1. **Протестируйте регистрацию и вход** в приложении
2. **Проверьте сохранение данных** в Firestore
3. **Настройте правила безопасности** для production (если нужно)
4. **Добавьте другие методы входа** (Google, Apple и т.д.), если нужно

---

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в Android Studio (Logcat)
2. Проверьте консоль Firebase на наличие ошибок
3. Убедитесь, что `google-services.json` находится в папке `app/`
4. Проверьте, что все зависимости синхронизированы в Gradle

---

**Готово!** Теперь ваше приложение готово к работе с Firebase! 🎉

