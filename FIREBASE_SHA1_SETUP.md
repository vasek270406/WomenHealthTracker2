# 🔑 Настройка SHA-1 Fingerprint для Firebase

## ❌ Проблема: DEVELOPER_ERROR

Ошибка `DEVELOPER_ERROR` означает, что Firebase не может проверить подлинность вашего приложения. Это происходит, когда SHA-1 fingerprint не добавлен в Firebase Console.

## ✅ Решение: Добавить SHA-1 Fingerprint

### Шаг 1: Получить SHA-1 Fingerprint

#### Для Windows:
```bash
cd %USERPROFILE%\.android
keytool -list -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android
```

#### Для macOS/Linux:
```bash
cd ~/.android
keytool -list -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android
```

#### Альтернативный способ (через Gradle):
1. В Android Studio откройте терминал (View → Tool Windows → Terminal)
2. Выполните команду:
```bash
./gradlew signingReport
```
3. Найдите SHA1 в разделе `Variant: debug` → `SHA1:`

### Шаг 2: Скопировать SHA-1

После выполнения команды найдите строку:
```
SHA1: XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX
```

Скопируйте значение SHA1 (без пробелов, только двоеточия и цифры/буквы).

### Шаг 3: Добавить SHA-1 в Firebase Console

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект **"womenht2"**
3. Перейдите в **Project Settings** (⚙️ → Project settings)
4. Прокрутите вниз до раздела **"Your apps"**
5. Найдите ваше Android приложение (`com.example.womenhealthtracker`)
6. Нажмите на иконку **"Add fingerprint"** (или "Add SHA certificate fingerprint")
7. Вставьте скопированный SHA-1 fingerprint
8. Нажмите **"Save"**

### Шаг 4: Скачать обновленный google-services.json

1. После добавления SHA-1, скачайте обновленный `google-services.json`
2. Замените старый файл в `app/google-services.json`
3. Синхронизируйте проект (Sync Project with Gradle Files)

### Шаг 5: Пересобрать проект

1. Build → Clean Project
2. Build → Rebuild Project
3. Запустите приложение заново

---

## 🔍 Проверка SHA-1 через Android Studio

### Способ 1: Gradle Task

1. Откройте **Gradle** панель (справа)
2. Разверните: **app → Tasks → android → signingReport**
3. Дважды кликните на **signingReport**
4. Внизу в **Run** панели найдите SHA1

### Способ 2: Terminal в Android Studio

1. View → Tool Windows → Terminal
2. Выполните:
```bash
./gradlew signingReport
```
3. Найдите SHA1 в выводе

---

## 📱 Для Release версии

Если вы создаете release версию, вам также нужно добавить SHA-1 от release keystore:

```bash
keytool -list -v -keystore путь/к/вашему/keystore.jks -alias ваш_alias
```

---

## ✅ Проверка

После добавления SHA-1:

1. ✅ Ошибка `DEVELOPER_ERROR` должна исчезнуть
2. ✅ Firebase Authentication должен работать
3. ✅ Firestore должен работать
4. ✅ В логах не должно быть ошибок от GoogleApiManager

---

## 🚨 Важно

- **SHA-1 для debug** - нужен для разработки и тестирования
- **SHA-1 для release** - нужен для production версии
- После добавления SHA-1 может потребоваться несколько минут для применения изменений

---

## 📝 Пример SHA-1

SHA-1 выглядит примерно так:
```
A1:B2:C3:D4:E5:F6:78:90:AB:CD:EF:12:34:56:78:90:AB:CD:EF:12
```

Скопируйте его полностью, включая двоеточия.

---

**После добавления SHA-1 перезапустите приложение и проверьте, что ошибка исчезла!**







