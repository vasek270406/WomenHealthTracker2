# 🔧 Исправление ошибки Google Play Services

## ❌ Ошибка: `SecurityException: Unknown calling package name 'com.google.android.gms'`

Эта ошибка появляется в логах, но **НЕ блокирует работу Firebase**. Приложение должно работать нормально.

## ✅ Решение

### Вариант 1: Игнорировать (рекомендуется)

Эта ошибка не влияет на работу Firebase Authentication и Firestore. Если приложение работает нормально (регистрация, вход, сохранение данных), можно просто игнорировать это предупреждение.

### Вариант 2: Добавить SHA-1 в Firebase Console

Если хотите убрать это предупреждение:

1. **Получите SHA-1 fingerprint:**

   ```bash
   cd ~/.android
   keytool -list -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android
   ```

   Или через Gradle:
   ```bash
   ./gradlew signingReport
   ```

2. **Добавьте SHA-1 в Firebase Console:**
   - Откройте [Firebase Console](https://console.firebase.google.com/)
   - Выберите проект **womenht2**
   - Project Settings (⚙️) → Your apps
   - Найдите Android приложение
   - Нажмите "Add fingerprint"
   - Вставьте SHA-1
   - Скачайте обновленный `google-services.json` (если нужно)

3. **Пересоберите приложение:**
   ```bash
   ./gradlew clean
   ./gradlew build
   ```

### Вариант 3: Проверить версию Google Play Services

Убедитесь, что на устройстве установлена актуальная версия Google Play Services:
- Откройте Google Play Store
- Найдите "Google Play Services"
- Обновите, если доступно обновление

## 🔍 Проверка работы Firebase

Если Firebase работает нормально, эта ошибка не критична. Проверьте:

1. **Регистрация работает?** → Firebase работает ✅
2. **Вход работает?** → Firebase работает ✅
3. **Данные сохраняются в Firestore?** → Firebase работает ✅

## 📝 Примечание

Эта ошибка часто появляется при:
- Первом запуске приложения
- Использовании debug-версии
- Отсутствии SHA-1 в Firebase Console

**Вывод:** Если приложение работает, можно игнорировать это предупреждение в логах.






