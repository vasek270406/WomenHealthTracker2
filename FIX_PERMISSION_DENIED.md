# 🔧 Исправление ошибки PERMISSION_DENIED

## ❌ Проблема

Ошибка `PERMISSION_DENIED: Missing or insufficient permissions` при сохранении данных беременности в Firestore.

**Причина:** Правила безопасности Firestore не включают подколлекцию `pregnancy` и другие подколлекции.

---

## ✅ Решение (5 минут)

### Шаг 1: Откройте Firebase Console
1. Перейдите на https://console.firebase.google.com/
2. Войдите в аккаунт
3. Выберите проект **"womenht2"**
4. Перейдите в **Firestore Database** → вкладка **"Rules"**

### Шаг 2: Замените правила безопасности

**Удалите ВСЁ содержимое** в редакторе правил и вставьте:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      match /days/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /pregnancy/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /fertility/{date} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /menopause_symptoms/{document=**} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /menopause_doctor_visits/{visitId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      match /mrs_results/{testDate} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
```

### Шаг 3: Опубликуйте правила
1. Нажмите кнопку **"Publish"** (Опубликовать)
2. Подождите 10-30 секунд
3. Убедитесь, что появилось сообщение об успешной публикации

### Шаг 4: Перезапустите приложение
1. Закройте приложение полностью
2. Запустите заново
3. Попробуйте сохранить визит к врачу

---

## ✅ Проверка

После исправления:
- ✅ Сохранение визитов к врачу работает
- ✅ Сохранение данных беременности работает
- ✅ Сохранение данных менопаузы работает
- ✅ Нет ошибок `PERMISSION_DENIED` в логах

---

## 📋 Что было исправлено

Добавлены правила безопасности для всех подколлекций:
- ✅ `days` - данные дня
- ✅ `pregnancy` - данные беременности
- ✅ `fertility` - данные фертильности
- ✅ `menopause_symptoms` - симптомы менопаузы
- ✅ `menopause_doctor_visits` - визиты к врачу при менопаузе
- ✅ `mrs_results` - результаты MRS

---

**После обновления правил перезапустите приложение!**






